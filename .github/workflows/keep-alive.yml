name: Keep Alive
on:
  schedule:
    - cron: '*/5 * * * *'  # Каждые 5 минут
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep server awake
        run: |
          echo "🚀 Пинг сервера в $(date)"
          # Простая версия без проверки ошибок
          curl -s https://elite-doors.shop/api/keep-alive || echo "Запрос выполнен (даже если с ошибкой)"
          echo "✅ Задача завершена в $(date)"